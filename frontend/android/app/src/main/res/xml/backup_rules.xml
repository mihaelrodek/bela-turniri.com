<?xml version="1.0" encoding="utf-8"?>
<!--
    Pre-Android-12 (API 23-30) counterpart of data_extraction_rules.xml,
    referenced from <application android:fullBackupContent>. Same decision,
    same reasoning — see data_extraction_rules.xml for the full justification.

    android:allowBackup="false" already makes this file moot on those API
    levels; it exists so the exclusions stay correct if backup is ever turned
    back on, instead of silently starting to ship the auth session and the FCM
    token to Google Drive.
-->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
    <exclude domain="device_root" path="." />
    <exclude domain="device_file" path="." />
    <exclude domain="device_database" path="." />
    <exclude domain="device_sharedpref" path="." />
</full-backup-content>
