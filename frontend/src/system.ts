import { createSystem, defaultConfig } from "@chakra-ui/react"

// start with defaults; customize later
export const system = createSystem(defaultConfig, {
    // example: add tokens later if you want brand colors, etc.
})