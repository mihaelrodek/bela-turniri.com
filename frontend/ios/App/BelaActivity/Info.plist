<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Info.plist of the BelaActivity widget extension.

  The extension exists only to draw the Live Activity declared in
  BelaActivityLiveActivity.swift. It has no App Group, no entitlements file
  and no capabilities of its own: all data arrives through ActivityKit, from
  the app (BelaLiveActivityPlugin) or from APNs.

  NSExtensionPointIdentifier MUST be com.apple.widgetkit-extension — with
  anything else the bundle installs but WidgetKit never loads it, and the
  Live Activity silently never appears.

  CFBundleVersion / CFBundleShortVersionString are wired to the same
  MARKETING_VERSION / CURRENT_PROJECT_VERSION build settings as the app; App
  Store validation rejects an embedded extension whose versions differ from
  its host app's.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Bela</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.widgetkit-extension</string>
	</dict>
</dict>
</plist>
